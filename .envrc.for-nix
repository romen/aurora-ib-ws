nix_direnv_manual_reload
#use flake .\?submodules=1 --impure
use flake .\?submodules=1

use_nodeenv() {
  nodeenv $PWD/.direnv/nodeenv
  . $PWD/.direnv/nodeenv/bin/activate
}

use nodeenv
layout ruby
